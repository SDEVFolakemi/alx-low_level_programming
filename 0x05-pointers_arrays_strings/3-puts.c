#include "main.h"

/**
 * _puts - prints a string
 * @str: string
 *
 * Return: the length of the string
 */
int _strlen(char *s)

	int longi - 0;

	while (*s !- '\0')
	{
		longi++;
		s++;
	}
	return(longi);

}
